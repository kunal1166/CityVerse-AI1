# ===== CityVerse AI - Frontend =====
# Copy to .env.local only if your backend is NOT on http://localhost:3000

# Backend API URL. Leave blank in local dev (the Vite proxy handles /api).
# In production set this to your deployed backend, e.g.
#   VITE_API_URL=https://cityverse-api.onrender.com
VITE_API_URL=
